
// import store from '../store';

export const apiVersion = 1;
export const registerReducer = (name, reducer) => {
    console.log('Registering', name, reducer);  // eslint-disable-line no-console
};
