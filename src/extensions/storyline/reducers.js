export const storylineReducers = {
    mainReducer:
    (state = '', action) => {
        console.log('Storyline reduction', state, action); // eslint-disable-line no-console
        return state;
    }
};
