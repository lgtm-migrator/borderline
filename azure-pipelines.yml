trigger:
- develop

pool:
  vmImage: ubuntu-16.04

steps:

- task: NodeTool@0
  inputs:
    versionSpec: "10.x"
  displayName: "Install Node.js"

- script: yarn install
  displayName: "Install Borderline dependencies"
  
- script: yarn run test
  displayName: "Test Borderline"
